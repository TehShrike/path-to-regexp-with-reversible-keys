{
  "name": "path-to-regexp-with-reversible-keys",
  "description": "A fork with an extra feature",
  "version": "1.0.1",
  "scripts": {
    "test": "istanbul cover node_modules/mocha/bin/_mocha -- -R spec"
  },
  "keywords": [
    "express",
    "regexp",
    "route",
    "routing"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/tehshrike/path-to-regexp-with-reversible-keys.git"
  },
  "devDependencies": {
    "istanbul": "~0.3.0",
    "mocha": "~1.21.4"
  }
}
